const getUser2 = () =>{
    return {
        uid: 'ABC123',
        username: 'El_Papi1502'
    }
}

const myNumbers: number[] = [1,2,3,4,5];

myNumbers.forEach( (number) => console.log( {number} ) );